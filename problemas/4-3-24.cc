/*

#include <iostream>

using namespace std;

int main() {
  cout << "Elementos de la submatriz triangular inferior sin diagonal: \n";
  bool is_symmetric() {
    bool is_sym = true;
    int m{};
    for (int i{0}; i < m; ++i) {
      for (int j{0}; j < i; ++j) {
        if(C[i][j] != C[j][i]) {
          is_sim = false;
        }
      }
    }
    return is_sym;
  }
  return 0;
}




void scalar_product() {
  double producto = 0;
  for (int i{0}; i < nz_; ++i) {
    producto += val_[i] * v[inx_[i]];

  }
}

void product() {
  double product = 0;
  for (int i{0}, j{0}; (i < sv1.nz_) && (j < sv2.nz_);) {
    if (sv1.inx_[i] == sv1.inx[j]) {

    }
  }
  cout << product << "\n"
}







void suma_vector() {
  double suma = 0;
  for (int i{0}, j{0}; (i < sv1.nz_) && (j < sv2.nz_);) {
    if (sv1.inx_[i] == sv2.inx_[j]) {
      suma += sv1.val_[i] + sv2.val_[j];
    }
    else if (sv1.inx_[i] < sv2.inx_[j]) {
      ++i;    
    }
    else {
      ++j;
    }
  }
}

*/