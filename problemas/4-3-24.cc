#include <iostream>

using namespace std;

int main() {
  cout << "Elementos de la submatriz triangular inferior sin diagonal: \n";
  bool is_symmetric() {
    bool is_sym = true;
    int m{};
    for (int i{0}; i < m; ++i) {
      for (int j{0}; j < i; ++j) {
        if(C[i][j] != C[j][i]) {
          is_sim = false;
        }
      }
    }
    return is_sym;
  }
  return 0;
}